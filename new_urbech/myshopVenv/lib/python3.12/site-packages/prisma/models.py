# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


from typing_extensions import LiteralString
# -- template models.py.jinja --
import os
import logging
import inspect
import warnings
from collections import OrderedDict

from pydantic import BaseModel, Field

from . import types, enums, errors, fields, bases
from ._types import FuncType
from ._compat import model_rebuild, field_validator
from .builder import serialize_base64
from .generator import partial_models_ctx, PartialModelField


log: logging.Logger = logging.getLogger(__name__)
_created_partial_types: Set[str] = set()

class User(bases.BaseUser):
    """Represents a User record"""

    id: _str
    name: _str
    email: _str
    hashedPassword: _str
    role: 'enums.Role'
    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    passwordChangedAt: Optional[datetime.datetime] = None
    verificationCode: Optional[_str] = None
    isVerified: _bool
    refreshToken: Optional[_str] = None
    userAddresses: Optional[List['models.UserAddress']] = None
    orders: Optional[List['models.Order']] = None
    customers: Optional[List['models.Customer']] = None
    passwordResetTokens: Optional[List['models.PasswordResetToken']] = None
    wishlist: Optional[List['models.WishlistItem']] = None
    lastLoginAt: Optional[datetime.datetime] = None
    deletedAt: Optional[datetime.datetime] = None
    Review: Optional[List['models.Review']] = None



    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.UserKeys']] = None,
        exclude: Optional[Iterable['types.UserKeys']] = None,
        required: Optional[Iterable['types.UserKeys']] = None,
        optional: Optional[Iterable['types.UserKeys']] = None,
        relations: Optional[Mapping['types.UserRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.UserKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _User_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _User_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _User_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _User_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _User_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _User_relational_fields:
                        raise errors.UnknownRelationalFieldError('User', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid User / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'User',
            }
        )
        _created_partial_types.add(name)


class PasswordResetToken(bases.BasePasswordResetToken):
    """Represents a PasswordResetToken record"""

    id: _str
    token: _str
    expiresAt: datetime.datetime
    userId: _str
    user: Optional['models.User'] = None
    createdAt: datetime.datetime



    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.PasswordResetTokenKeys']] = None,
        exclude: Optional[Iterable['types.PasswordResetTokenKeys']] = None,
        required: Optional[Iterable['types.PasswordResetTokenKeys']] = None,
        optional: Optional[Iterable['types.PasswordResetTokenKeys']] = None,
        relations: Optional[Mapping['types.PasswordResetTokenRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.PasswordResetTokenKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _PasswordResetToken_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _PasswordResetToken_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _PasswordResetToken_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _PasswordResetToken_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _PasswordResetToken_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _PasswordResetToken_relational_fields:
                        raise errors.UnknownRelationalFieldError('PasswordResetToken', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid PasswordResetToken / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'PasswordResetToken',
            }
        )
        _created_partial_types.add(name)


class Address(bases.BaseAddress):
    """Represents a Address record"""

    id: _str
    label: Optional[_str] = None
    company: Optional[_str] = None
    firstName: Optional[_str] = None
    lastName: Optional[_str] = None
    street1: _str
    street2: Optional[_str] = None
    city: _str
    state: Optional[_str] = None
    zip: Optional[_str] = None
    country: _str
    countryCode: Optional[_str] = None
    phone: Optional[_str] = None
    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    userAddresses: Optional[List['models.UserAddress']] = None
    shippingOrders: Optional[List['models.Order']] = None
    billingOrders: Optional[List['models.Order']] = None
    products: Optional[List['models.Product']] = None
    partners: Optional[List['models.Partner']] = None
    Customer: Optional[List['models.Customer']] = None



    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.AddressKeys']] = None,
        exclude: Optional[Iterable['types.AddressKeys']] = None,
        required: Optional[Iterable['types.AddressKeys']] = None,
        optional: Optional[Iterable['types.AddressKeys']] = None,
        relations: Optional[Mapping['types.AddressRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.AddressKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Address_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Address_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Address_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Address_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Address_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Address_relational_fields:
                        raise errors.UnknownRelationalFieldError('Address', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Address / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Address',
            }
        )
        _created_partial_types.add(name)


class UserAddress(bases.BaseUserAddress):
    """Represents a UserAddress record"""

    id: _str
    userId: _str
    addressId: _str
    isDefaultShipping: _bool
    isDefaultBilling: _bool
    createdAt: datetime.datetime
    user: Optional['models.User'] = None
    address: Optional['models.Address'] = None



    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.UserAddressKeys']] = None,
        exclude: Optional[Iterable['types.UserAddressKeys']] = None,
        required: Optional[Iterable['types.UserAddressKeys']] = None,
        optional: Optional[Iterable['types.UserAddressKeys']] = None,
        relations: Optional[Mapping['types.UserAddressRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.UserAddressKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _UserAddress_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _UserAddress_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _UserAddress_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _UserAddress_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _UserAddress_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _UserAddress_relational_fields:
                        raise errors.UnknownRelationalFieldError('UserAddress', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid UserAddress / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'UserAddress',
            }
        )
        _created_partial_types.add(name)


class Product(bases.BaseProduct):
    """Represents a Product record"""

    id: _str
    name: _str
    slug: _str
    description: Optional[_str] = None
    status: 'enums.PublishStatus'
    price: Optional[decimal.Decimal] = None
    currencyCode: Optional[_str] = None
    stock: _int
    minStock: _int
    isLowStock: _bool
    articleNumber: Optional[_str] = None
    weightGrams: Optional[_int] = None
    lengthCm: Optional[decimal.Decimal] = None
    widthCm: Optional[decimal.Decimal] = None
    heightCm: Optional[decimal.Decimal] = None
    color: Optional[_str] = None
    mainImageUrl: Optional[_str] = None
    images: Optional[List['models.ProductImage']] = None
    categories: Optional[List['models.ProductCategory']] = None
    tags: Optional[List['models.ProductTag']] = None
    variants: Optional[List['models.ProductVariant']] = None
    attributes: Optional[List['models.ProductAttribute']] = None
    orderItems: Optional[List['models.OrderItem']] = None
    wishlistedBy: Optional[List['models.WishlistItem']] = None
    originAddressId: Optional[_str] = None
    originAddress: Optional['models.Address'] = None
    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    deletedAt: Optional[datetime.datetime] = None
    Review: Optional[List['models.Review']] = None



    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ProductKeys']] = None,
        exclude: Optional[Iterable['types.ProductKeys']] = None,
        required: Optional[Iterable['types.ProductKeys']] = None,
        optional: Optional[Iterable['types.ProductKeys']] = None,
        relations: Optional[Mapping['types.ProductRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ProductKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Product_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Product_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Product_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Product_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Product_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Product_relational_fields:
                        raise errors.UnknownRelationalFieldError('Product', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Product / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Product',
            }
        )
        _created_partial_types.add(name)


class ProductImage(bases.BaseProductImage):
    """Represents a ProductImage record"""

    id: _str
    productId: _str
    url: _str
    altText: Optional[_str] = None
    isPrimary: _bool
    sortOrder: _int
    product: Optional['models.Product'] = None



    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ProductImageKeys']] = None,
        exclude: Optional[Iterable['types.ProductImageKeys']] = None,
        required: Optional[Iterable['types.ProductImageKeys']] = None,
        optional: Optional[Iterable['types.ProductImageKeys']] = None,
        relations: Optional[Mapping['types.ProductImageRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ProductImageKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ProductImage_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ProductImage_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ProductImage_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ProductImage_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ProductImage_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ProductImage_relational_fields:
                        raise errors.UnknownRelationalFieldError('ProductImage', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ProductImage / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ProductImage',
            }
        )
        _created_partial_types.add(name)


class ProductVariant(bases.BaseProductVariant):
    """Represents a ProductVariant record"""

    id: _str
    productId: _str
    sku: _str
    barcode: Optional[_str] = None
    price: decimal.Decimal
    currencyCode: _str
    stock: _int
    minStock: _int
    weightGrams: Optional[_int] = None
    imageUrl: Optional[_str] = None
    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    product: Optional['models.Product'] = None
    options: Optional[List['models.ProductVariantOptionValue']] = None
    stockMoves: Optional[List['models.StockMovement']] = None
    reservations: Optional[List['models.InventoryReservation']] = None
    OrderItem: Optional[List['models.OrderItem']] = None



    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ProductVariantKeys']] = None,
        exclude: Optional[Iterable['types.ProductVariantKeys']] = None,
        required: Optional[Iterable['types.ProductVariantKeys']] = None,
        optional: Optional[Iterable['types.ProductVariantKeys']] = None,
        relations: Optional[Mapping['types.ProductVariantRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ProductVariantKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ProductVariant_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ProductVariant_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ProductVariant_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ProductVariant_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ProductVariant_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ProductVariant_relational_fields:
                        raise errors.UnknownRelationalFieldError('ProductVariant', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ProductVariant / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ProductVariant',
            }
        )
        _created_partial_types.add(name)


class ProductOption(bases.BaseProductOption):
    """Represents a ProductOption record"""

    id: _str
    name: _str
    slug: _str
    values: Optional[List['models.ProductOptionValue']] = None



    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ProductOptionKeys']] = None,
        exclude: Optional[Iterable['types.ProductOptionKeys']] = None,
        required: Optional[Iterable['types.ProductOptionKeys']] = None,
        optional: Optional[Iterable['types.ProductOptionKeys']] = None,
        relations: Optional[Mapping['types.ProductOptionRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ProductOptionKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ProductOption_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ProductOption_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ProductOption_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ProductOption_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ProductOption_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ProductOption_relational_fields:
                        raise errors.UnknownRelationalFieldError('ProductOption', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ProductOption / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ProductOption',
            }
        )
        _created_partial_types.add(name)


class ProductOptionValue(bases.BaseProductOptionValue):
    """Represents a ProductOptionValue record"""

    id: _str
    optionId: _str
    value: _str
    option: Optional['models.ProductOption'] = None
    variants: Optional[List['models.ProductVariantOptionValue']] = None



    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ProductOptionValueKeys']] = None,
        exclude: Optional[Iterable['types.ProductOptionValueKeys']] = None,
        required: Optional[Iterable['types.ProductOptionValueKeys']] = None,
        optional: Optional[Iterable['types.ProductOptionValueKeys']] = None,
        relations: Optional[Mapping['types.ProductOptionValueRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ProductOptionValueKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ProductOptionValue_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ProductOptionValue_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ProductOptionValue_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ProductOptionValue_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ProductOptionValue_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ProductOptionValue_relational_fields:
                        raise errors.UnknownRelationalFieldError('ProductOptionValue', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ProductOptionValue / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ProductOptionValue',
            }
        )
        _created_partial_types.add(name)


class ProductVariantOptionValue(bases.BaseProductVariantOptionValue):
    """Represents a ProductVariantOptionValue record"""

    variantId: _str
    optionValueId: _str
    variant: Optional['models.ProductVariant'] = None
    optionValue: Optional['models.ProductOptionValue'] = None



    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ProductVariantOptionValueKeys']] = None,
        exclude: Optional[Iterable['types.ProductVariantOptionValueKeys']] = None,
        required: Optional[Iterable['types.ProductVariantOptionValueKeys']] = None,
        optional: Optional[Iterable['types.ProductVariantOptionValueKeys']] = None,
        relations: Optional[Mapping['types.ProductVariantOptionValueRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ProductVariantOptionValueKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ProductVariantOptionValue_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ProductVariantOptionValue_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ProductVariantOptionValue_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ProductVariantOptionValue_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ProductVariantOptionValue_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ProductVariantOptionValue_relational_fields:
                        raise errors.UnknownRelationalFieldError('ProductVariantOptionValue', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ProductVariantOptionValue / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ProductVariantOptionValue',
            }
        )
        _created_partial_types.add(name)


class Category(bases.BaseCategory):
    """Represents a Category record"""

    id: _str
    name: _str
    slug: _str
    defaultSortOrder: _int
    parentId: Optional[_str] = None
    parent: Optional['models.Category'] = None
    children: Optional[List['models.Category']] = None
    products: Optional[List['models.ProductCategory']] = None
    path: Optional[_str] = None
    createdAt: datetime.datetime
    updatedAt: datetime.datetime



    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.CategoryKeys']] = None,
        exclude: Optional[Iterable['types.CategoryKeys']] = None,
        required: Optional[Iterable['types.CategoryKeys']] = None,
        optional: Optional[Iterable['types.CategoryKeys']] = None,
        relations: Optional[Mapping['types.CategoryRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.CategoryKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Category_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Category_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Category_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Category_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Category_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Category_relational_fields:
                        raise errors.UnknownRelationalFieldError('Category', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Category / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Category',
            }
        )
        _created_partial_types.add(name)


class ProductCategory(bases.BaseProductCategory):
    """Represents a ProductCategory record"""

    productId: _str
    categoryId: _str
    featured: _bool
    sortOrder: _int
    product: Optional['models.Product'] = None
    category: Optional['models.Category'] = None



    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ProductCategoryKeys']] = None,
        exclude: Optional[Iterable['types.ProductCategoryKeys']] = None,
        required: Optional[Iterable['types.ProductCategoryKeys']] = None,
        optional: Optional[Iterable['types.ProductCategoryKeys']] = None,
        relations: Optional[Mapping['types.ProductCategoryRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ProductCategoryKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ProductCategory_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ProductCategory_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ProductCategory_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ProductCategory_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ProductCategory_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ProductCategory_relational_fields:
                        raise errors.UnknownRelationalFieldError('ProductCategory', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ProductCategory / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ProductCategory',
            }
        )
        _created_partial_types.add(name)


class Attribute(bases.BaseAttribute):
    """Represents a Attribute record"""

    id: _str
    name: _str
    slug: _str
    type: Optional[_str] = None
    values: Optional[List['models.ProductAttribute']] = None



    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.AttributeKeys']] = None,
        exclude: Optional[Iterable['types.AttributeKeys']] = None,
        required: Optional[Iterable['types.AttributeKeys']] = None,
        optional: Optional[Iterable['types.AttributeKeys']] = None,
        relations: Optional[Mapping['types.AttributeRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.AttributeKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Attribute_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Attribute_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Attribute_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Attribute_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Attribute_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Attribute_relational_fields:
                        raise errors.UnknownRelationalFieldError('Attribute', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Attribute / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Attribute',
            }
        )
        _created_partial_types.add(name)


class ProductAttribute(bases.BaseProductAttribute):
    """Represents a ProductAttribute record"""

    productId: _str
    attributeId: _str
    valueText: Optional[_str] = None
    valueNumber: Optional[decimal.Decimal] = None
    valueBool: Optional[_bool] = None
    product: Optional['models.Product'] = None
    attribute: Optional['models.Attribute'] = None



    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ProductAttributeKeys']] = None,
        exclude: Optional[Iterable['types.ProductAttributeKeys']] = None,
        required: Optional[Iterable['types.ProductAttributeKeys']] = None,
        optional: Optional[Iterable['types.ProductAttributeKeys']] = None,
        relations: Optional[Mapping['types.ProductAttributeRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ProductAttributeKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ProductAttribute_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ProductAttribute_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ProductAttribute_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ProductAttribute_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ProductAttribute_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ProductAttribute_relational_fields:
                        raise errors.UnknownRelationalFieldError('ProductAttribute', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ProductAttribute / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ProductAttribute',
            }
        )
        _created_partial_types.add(name)


class StockMovement(bases.BaseStockMovement):
    """Represents a StockMovement record"""

    id: _str
    variantId: _str
    delta: _int
    reason: _str
    referenceId: Optional[_str] = None
    createdAt: datetime.datetime
    variant: Optional['models.ProductVariant'] = None



    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.StockMovementKeys']] = None,
        exclude: Optional[Iterable['types.StockMovementKeys']] = None,
        required: Optional[Iterable['types.StockMovementKeys']] = None,
        optional: Optional[Iterable['types.StockMovementKeys']] = None,
        relations: Optional[Mapping['types.StockMovementRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.StockMovementKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _StockMovement_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _StockMovement_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _StockMovement_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _StockMovement_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _StockMovement_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _StockMovement_relational_fields:
                        raise errors.UnknownRelationalFieldError('StockMovement', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid StockMovement / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'StockMovement',
            }
        )
        _created_partial_types.add(name)


class InventoryReservation(bases.BaseInventoryReservation):
    """Represents a InventoryReservation record"""

    id: _str
    variantId: _str
    orderId: Optional[_str] = None
    quantity: _int
    expiresAt: datetime.datetime
    createdAt: datetime.datetime
    variant: Optional['models.ProductVariant'] = None
    order: Optional['models.Order'] = None



    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.InventoryReservationKeys']] = None,
        exclude: Optional[Iterable['types.InventoryReservationKeys']] = None,
        required: Optional[Iterable['types.InventoryReservationKeys']] = None,
        optional: Optional[Iterable['types.InventoryReservationKeys']] = None,
        relations: Optional[Mapping['types.InventoryReservationRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.InventoryReservationKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _InventoryReservation_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _InventoryReservation_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _InventoryReservation_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _InventoryReservation_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _InventoryReservation_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _InventoryReservation_relational_fields:
                        raise errors.UnknownRelationalFieldError('InventoryReservation', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid InventoryReservation / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'InventoryReservation',
            }
        )
        _created_partial_types.add(name)


class WishlistItem(bases.BaseWishlistItem):
    """Represents a WishlistItem record"""

    userId: _str
    productId: _str
    createdAt: datetime.datetime
    user: Optional['models.User'] = None
    product: Optional['models.Product'] = None



    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.WishlistItemKeys']] = None,
        exclude: Optional[Iterable['types.WishlistItemKeys']] = None,
        required: Optional[Iterable['types.WishlistItemKeys']] = None,
        optional: Optional[Iterable['types.WishlistItemKeys']] = None,
        relations: Optional[Mapping['types.WishlistItemRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.WishlistItemKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _WishlistItem_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _WishlistItem_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _WishlistItem_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _WishlistItem_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _WishlistItem_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _WishlistItem_relational_fields:
                        raise errors.UnknownRelationalFieldError('WishlistItem', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid WishlistItem / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'WishlistItem',
            }
        )
        _created_partial_types.add(name)


class Order(bases.BaseOrder):
    """Represents a Order record"""

    id: _str
    orderNumber: _str
    userId: Optional[_str] = None
    customerId: Optional[_str] = None
    partnerId: Optional[_str] = None
    currencyCode: _str
    subtotal: decimal.Decimal
    discountTotal: decimal.Decimal
    shippingTotal: decimal.Decimal
    taxTotal: decimal.Decimal
    total: decimal.Decimal
    status: 'enums.OrderStatus'
    paymentStatus: 'enums.PaymentStatus'
    fulfillment: 'enums.FulfillmentStatus'
    shippingAddressId: Optional[_str] = None
    billingAddressId: Optional[_str] = None
    shippingAddress: Optional['models.Address'] = None
    billingAddress: Optional['models.Address'] = None
    customerEmail: Optional[_str] = None
    customerName: Optional[_str] = None
    paymentMode: Optional[_str] = None
    sendingAgent: Optional[_str] = None
    city: Optional[_str] = None
    location: Optional[_str] = None
    items: Optional[List['models.OrderItem']] = None
    payments: Optional[List['models.Payment']] = None
    shipments: Optional[List['models.Shipment']] = None
    partner: Optional['models.Partner'] = None
    customer: Optional['models.Customer'] = None
    user: Optional['models.User'] = None
    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    canceledAt: Optional[datetime.datetime] = None
    InventoryReservation: Optional[List['models.InventoryReservation']] = None



    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.OrderKeys']] = None,
        exclude: Optional[Iterable['types.OrderKeys']] = None,
        required: Optional[Iterable['types.OrderKeys']] = None,
        optional: Optional[Iterable['types.OrderKeys']] = None,
        relations: Optional[Mapping['types.OrderRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.OrderKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Order_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Order_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Order_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Order_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Order_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Order_relational_fields:
                        raise errors.UnknownRelationalFieldError('Order', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Order / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Order',
            }
        )
        _created_partial_types.add(name)


class OrderItem(bases.BaseOrderItem):
    """Represents a OrderItem record"""

    id: _str
    orderId: _str
    productId: Optional[_str] = None
    variantId: Optional[_str] = None
    name: _str
    sku: Optional[_str] = None
    unitPrice: decimal.Decimal
    quantity: _int
    taxRate: Optional[decimal.Decimal] = None
    discount: decimal.Decimal
    total: decimal.Decimal
    order: Optional['models.Order'] = None
    product: Optional['models.Product'] = None
    variant: Optional['models.ProductVariant'] = None
    shipments: Optional[List['models.ShipmentItem']] = None



    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.OrderItemKeys']] = None,
        exclude: Optional[Iterable['types.OrderItemKeys']] = None,
        required: Optional[Iterable['types.OrderItemKeys']] = None,
        optional: Optional[Iterable['types.OrderItemKeys']] = None,
        relations: Optional[Mapping['types.OrderItemRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.OrderItemKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _OrderItem_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _OrderItem_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _OrderItem_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _OrderItem_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _OrderItem_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _OrderItem_relational_fields:
                        raise errors.UnknownRelationalFieldError('OrderItem', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid OrderItem / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'OrderItem',
            }
        )
        _created_partial_types.add(name)


class Payment(bases.BasePayment):
    """Represents a Payment record"""

    id: _str
    orderId: _str
    provider: _str
    amount: decimal.Decimal
    currencyCode: _str
    status: 'enums.PaymentStatus'
    transactionId: Optional[_str] = None
    rawData: Optional['fields.Json'] = None
    createdAt: datetime.datetime
    order: Optional['models.Order'] = None



    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.PaymentKeys']] = None,
        exclude: Optional[Iterable['types.PaymentKeys']] = None,
        required: Optional[Iterable['types.PaymentKeys']] = None,
        optional: Optional[Iterable['types.PaymentKeys']] = None,
        relations: Optional[Mapping['types.PaymentRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.PaymentKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Payment_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Payment_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Payment_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Payment_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Payment_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Payment_relational_fields:
                        raise errors.UnknownRelationalFieldError('Payment', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Payment / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Payment',
            }
        )
        _created_partial_types.add(name)


class Shipment(bases.BaseShipment):
    """Represents a Shipment record"""

    id: _str
    orderId: _str
    carrier: Optional[_str] = None
    trackingCode: Optional[_str] = None
    trackingUrl: Optional[_str] = None
    status: Optional[_str] = None
    shippedAt: Optional[datetime.datetime] = None
    deliveredAt: Optional[datetime.datetime] = None
    items: Optional[List['models.ShipmentItem']] = None
    createdAt: datetime.datetime
    order: Optional['models.Order'] = None



    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ShipmentKeys']] = None,
        exclude: Optional[Iterable['types.ShipmentKeys']] = None,
        required: Optional[Iterable['types.ShipmentKeys']] = None,
        optional: Optional[Iterable['types.ShipmentKeys']] = None,
        relations: Optional[Mapping['types.ShipmentRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ShipmentKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Shipment_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Shipment_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Shipment_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Shipment_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Shipment_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Shipment_relational_fields:
                        raise errors.UnknownRelationalFieldError('Shipment', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Shipment / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Shipment',
            }
        )
        _created_partial_types.add(name)


class ShipmentItem(bases.BaseShipmentItem):
    """Represents a ShipmentItem record"""

    shipmentId: _str
    orderItemId: _str
    quantity: _int
    shipment: Optional['models.Shipment'] = None
    orderItem: Optional['models.OrderItem'] = None



    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ShipmentItemKeys']] = None,
        exclude: Optional[Iterable['types.ShipmentItemKeys']] = None,
        required: Optional[Iterable['types.ShipmentItemKeys']] = None,
        optional: Optional[Iterable['types.ShipmentItemKeys']] = None,
        relations: Optional[Mapping['types.ShipmentItemRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ShipmentItemKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ShipmentItem_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ShipmentItem_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ShipmentItem_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ShipmentItem_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ShipmentItem_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ShipmentItem_relational_fields:
                        raise errors.UnknownRelationalFieldError('ShipmentItem', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ShipmentItem / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ShipmentItem',
            }
        )
        _created_partial_types.add(name)


class Tag(bases.BaseTag):
    """Represents a Tag record"""

    id: _str
    name: _str
    slug: _str
    products: Optional[List['models.ProductTag']] = None



    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.TagKeys']] = None,
        exclude: Optional[Iterable['types.TagKeys']] = None,
        required: Optional[Iterable['types.TagKeys']] = None,
        optional: Optional[Iterable['types.TagKeys']] = None,
        relations: Optional[Mapping['types.TagRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.TagKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Tag_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Tag_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Tag_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Tag_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Tag_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Tag_relational_fields:
                        raise errors.UnknownRelationalFieldError('Tag', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Tag / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Tag',
            }
        )
        _created_partial_types.add(name)


class ProductTag(bases.BaseProductTag):
    """Represents a ProductTag record"""

    productId: _str
    tagId: _str
    product: Optional['models.Product'] = None
    tag: Optional['models.Tag'] = None



    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ProductTagKeys']] = None,
        exclude: Optional[Iterable['types.ProductTagKeys']] = None,
        required: Optional[Iterable['types.ProductTagKeys']] = None,
        optional: Optional[Iterable['types.ProductTagKeys']] = None,
        relations: Optional[Mapping['types.ProductTagRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ProductTagKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ProductTag_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ProductTag_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ProductTag_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ProductTag_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ProductTag_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ProductTag_relational_fields:
                        raise errors.UnknownRelationalFieldError('ProductTag', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ProductTag / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ProductTag',
            }
        )
        _created_partial_types.add(name)


class Coupon(bases.BaseCoupon):
    """Represents a Coupon record"""

    id: _str
    code: _str
    type: _str
    value: decimal.Decimal
    currencyCode: Optional[_str] = None
    startsAt: Optional[datetime.datetime] = None
    endsAt: Optional[datetime.datetime] = None
    maxRedemptions: Optional[_int] = None
    timesRedeemed: _int
    isActive: _bool
    createdAt: datetime.datetime



    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.CouponKeys']] = None,
        exclude: Optional[Iterable['types.CouponKeys']] = None,
        required: Optional[Iterable['types.CouponKeys']] = None,
        optional: Optional[Iterable['types.CouponKeys']] = None,
        relations: Optional[Mapping['types.CouponRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.CouponKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Coupon_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Coupon_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Coupon_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Coupon_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True


            if relations:
                raise ValueError('Model: "Coupon" has no relational fields.')
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Coupon / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Coupon',
            }
        )
        _created_partial_types.add(name)


class Review(bases.BaseReview):
    """Represents a Review record"""

    id: _str
    productId: _str
    userId: Optional[_str] = None
    rating: _int
    title: Optional[_str] = None
    body: Optional[_str] = None
    isPublished: _bool
    createdAt: datetime.datetime
    product: Optional['models.Product'] = None
    user: Optional['models.User'] = None



    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ReviewKeys']] = None,
        exclude: Optional[Iterable['types.ReviewKeys']] = None,
        required: Optional[Iterable['types.ReviewKeys']] = None,
        optional: Optional[Iterable['types.ReviewKeys']] = None,
        relations: Optional[Mapping['types.ReviewRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ReviewKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Review_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Review_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Review_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Review_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Review_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Review_relational_fields:
                        raise errors.UnknownRelationalFieldError('Review', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Review / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Review',
            }
        )
        _created_partial_types.add(name)


class Customer(bases.BaseCustomer):
    """Represents a Customer record"""

    id: _str
    name: _str
    email: _str
    phone: Optional[_str] = None
    group: Optional[_str] = None
    addressId: Optional[_str] = None
    address: Optional['models.Address'] = None
    city: Optional[_str] = None
    country: Optional[_str] = None
    orders: Optional[List['models.Order']] = None
    createdAt: datetime.datetime
    userId: Optional[_str] = None
    user: Optional['models.User'] = None



    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.CustomerKeys']] = None,
        exclude: Optional[Iterable['types.CustomerKeys']] = None,
        required: Optional[Iterable['types.CustomerKeys']] = None,
        optional: Optional[Iterable['types.CustomerKeys']] = None,
        relations: Optional[Mapping['types.CustomerRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.CustomerKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Customer_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Customer_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Customer_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Customer_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Customer_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Customer_relational_fields:
                        raise errors.UnknownRelationalFieldError('Customer', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Customer / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Customer',
            }
        )
        _created_partial_types.add(name)


class Partner(bases.BasePartner):
    """Represents a Partner record"""

    id: _str
    name: _str
    type: 'enums.PartnerType'
    contactName: Optional[_str] = None
    phone: Optional[_str] = None
    email: Optional[_str] = None
    addressId: Optional[_str] = None
    address: Optional['models.Address'] = None
    orders: Optional[List['models.Order']] = None
    createdAt: datetime.datetime
    updatedAt: datetime.datetime
    headerTitle: Optional[_str] = None
    footerNote: Optional[_str] = None
    bankDetails: Optional[_str] = None
    contactInfo: Optional[_str] = None
    logoUrl: Optional[_str] = None
    paymentTerms: Optional[_str] = None
    returnPolicy: Optional[_str] = None



    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.PartnerKeys']] = None,
        exclude: Optional[Iterable['types.PartnerKeys']] = None,
        required: Optional[Iterable['types.PartnerKeys']] = None,
        optional: Optional[Iterable['types.PartnerKeys']] = None,
        relations: Optional[Mapping['types.PartnerRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.PartnerKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Partner_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Partner_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Partner_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Partner_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Partner_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Partner_relational_fields:
                        raise errors.UnknownRelationalFieldError('Partner', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Partner / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Partner',
            }
        )
        _created_partial_types.add(name)



_User_relational_fields: Set[str] = {
        'userAddresses',
        'orders',
        'customers',
        'passwordResetTokens',
        'wishlist',
        'Review',
    }
_User_fields: Dict['types.UserKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('email', {
            'name': 'email',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('hashedPassword', {
            'name': 'hashedPassword',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('role', {
            'name': 'role',
            'is_list': False,
            'optional': False,
            'type': 'enums.Role',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('passwordChangedAt', {
            'name': 'passwordChangedAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('verificationCode', {
            'name': 'verificationCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('isVerified', {
            'name': 'isVerified',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('refreshToken', {
            'name': 'refreshToken',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('userAddresses', {
            'name': 'userAddresses',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.UserAddress\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('orders', {
            'name': 'orders',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Order\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('customers', {
            'name': 'customers',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Customer\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('passwordResetTokens', {
            'name': 'passwordResetTokens',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.PasswordResetToken\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('wishlist', {
            'name': 'wishlist',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.WishlistItem\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('lastLoginAt', {
            'name': 'lastLoginAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('deletedAt', {
            'name': 'deletedAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('Review', {
            'name': 'Review',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Review\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_PasswordResetToken_relational_fields: Set[str] = {
        'user',
    }
_PasswordResetToken_fields: Dict['types.PasswordResetTokenKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('token', {
            'name': 'token',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('expiresAt', {
            'name': 'expiresAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('userId', {
            'name': 'userId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('user', {
            'name': 'user',
            'is_list': False,
            'optional': True,
            'type': 'models.User',
            'is_relational': True,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)

_Address_relational_fields: Set[str] = {
        'userAddresses',
        'shippingOrders',
        'billingOrders',
        'products',
        'partners',
        'Customer',
    }
_Address_fields: Dict['types.AddressKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('label', {
            'name': 'label',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('firstName', {
            'name': 'firstName',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('lastName', {
            'name': 'lastName',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('street1', {
            'name': 'street1',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('street2', {
            'name': 'street2',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('city', {
            'name': 'city',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('state', {
            'name': 'state',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('zip', {
            'name': 'zip',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('country', {
            'name': 'country',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('countryCode', {
            'name': 'countryCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('phone', {
            'name': 'phone',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('userAddresses', {
            'name': 'userAddresses',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.UserAddress\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('shippingOrders', {
            'name': 'shippingOrders',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Order\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('billingOrders', {
            'name': 'billingOrders',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Order\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('products', {
            'name': 'products',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Product\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('partners', {
            'name': 'partners',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Partner\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('Customer', {
            'name': 'Customer',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Customer\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_UserAddress_relational_fields: Set[str] = {
        'user',
        'address',
    }
_UserAddress_fields: Dict['types.UserAddressKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('userId', {
            'name': 'userId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('addressId', {
            'name': 'addressId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('isDefaultShipping', {
            'name': 'isDefaultShipping',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('isDefaultBilling', {
            'name': 'isDefaultBilling',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('user', {
            'name': 'user',
            'is_list': False,
            'optional': True,
            'type': 'models.User',
            'is_relational': True,
            'documentation': None,
        }),
        ('address', {
            'name': 'address',
            'is_list': False,
            'optional': True,
            'type': 'models.Address',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Product_relational_fields: Set[str] = {
        'images',
        'categories',
        'tags',
        'variants',
        'attributes',
        'orderItems',
        'wishlistedBy',
        'originAddress',
        'Review',
    }
_Product_fields: Dict['types.ProductKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('slug', {
            'name': 'slug',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('description', {
            'name': 'description',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': 'enums.PublishStatus',
            'is_relational': False,
            'documentation': None,
        }),
        ('price', {
            'name': 'price',
            'is_list': False,
            'optional': True,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('currencyCode', {
            'name': 'currencyCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('stock', {
            'name': 'stock',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('minStock', {
            'name': 'minStock',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('isLowStock', {
            'name': 'isLowStock',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('articleNumber', {
            'name': 'articleNumber',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('weightGrams', {
            'name': 'weightGrams',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('lengthCm', {
            'name': 'lengthCm',
            'is_list': False,
            'optional': True,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('widthCm', {
            'name': 'widthCm',
            'is_list': False,
            'optional': True,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('heightCm', {
            'name': 'heightCm',
            'is_list': False,
            'optional': True,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('color', {
            'name': 'color',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('mainImageUrl', {
            'name': 'mainImageUrl',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('images', {
            'name': 'images',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ProductImage\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('categories', {
            'name': 'categories',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ProductCategory\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('tags', {
            'name': 'tags',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ProductTag\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('variants', {
            'name': 'variants',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ProductVariant\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('attributes', {
            'name': 'attributes',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ProductAttribute\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('orderItems', {
            'name': 'orderItems',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.OrderItem\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('wishlistedBy', {
            'name': 'wishlistedBy',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.WishlistItem\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('originAddressId', {
            'name': 'originAddressId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('originAddress', {
            'name': 'originAddress',
            'is_list': False,
            'optional': True,
            'type': 'models.Address',
            'is_relational': True,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('deletedAt', {
            'name': 'deletedAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('Review', {
            'name': 'Review',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Review\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_ProductImage_relational_fields: Set[str] = {
        'product',
    }
_ProductImage_fields: Dict['types.ProductImageKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('productId', {
            'name': 'productId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('url', {
            'name': 'url',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('altText', {
            'name': 'altText',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('isPrimary', {
            'name': 'isPrimary',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('sortOrder', {
            'name': 'sortOrder',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('product', {
            'name': 'product',
            'is_list': False,
            'optional': True,
            'type': 'models.Product',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_ProductVariant_relational_fields: Set[str] = {
        'product',
        'options',
        'stockMoves',
        'reservations',
        'OrderItem',
    }
_ProductVariant_fields: Dict['types.ProductVariantKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('productId', {
            'name': 'productId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('sku', {
            'name': 'sku',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('barcode', {
            'name': 'barcode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('price', {
            'name': 'price',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('currencyCode', {
            'name': 'currencyCode',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('stock', {
            'name': 'stock',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('minStock', {
            'name': 'minStock',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('weightGrams', {
            'name': 'weightGrams',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('imageUrl', {
            'name': 'imageUrl',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('product', {
            'name': 'product',
            'is_list': False,
            'optional': True,
            'type': 'models.Product',
            'is_relational': True,
            'documentation': None,
        }),
        ('options', {
            'name': 'options',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ProductVariantOptionValue\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('stockMoves', {
            'name': 'stockMoves',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.StockMovement\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('reservations', {
            'name': 'reservations',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.InventoryReservation\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('OrderItem', {
            'name': 'OrderItem',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.OrderItem\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_ProductOption_relational_fields: Set[str] = {
        'values',
    }
_ProductOption_fields: Dict['types.ProductOptionKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('slug', {
            'name': 'slug',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('values', {
            'name': 'values',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ProductOptionValue\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_ProductOptionValue_relational_fields: Set[str] = {
        'option',
        'variants',
    }
_ProductOptionValue_fields: Dict['types.ProductOptionValueKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('optionId', {
            'name': 'optionId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('value', {
            'name': 'value',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('option', {
            'name': 'option',
            'is_list': False,
            'optional': True,
            'type': 'models.ProductOption',
            'is_relational': True,
            'documentation': None,
        }),
        ('variants', {
            'name': 'variants',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ProductVariantOptionValue\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_ProductVariantOptionValue_relational_fields: Set[str] = {
        'variant',
        'optionValue',
    }
_ProductVariantOptionValue_fields: Dict['types.ProductVariantOptionValueKeys', PartialModelField] = OrderedDict(
    [
        ('variantId', {
            'name': 'variantId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('optionValueId', {
            'name': 'optionValueId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('variant', {
            'name': 'variant',
            'is_list': False,
            'optional': True,
            'type': 'models.ProductVariant',
            'is_relational': True,
            'documentation': None,
        }),
        ('optionValue', {
            'name': 'optionValue',
            'is_list': False,
            'optional': True,
            'type': 'models.ProductOptionValue',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Category_relational_fields: Set[str] = {
        'parent',
        'children',
        'products',
    }
_Category_fields: Dict['types.CategoryKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('slug', {
            'name': 'slug',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('defaultSortOrder', {
            'name': 'defaultSortOrder',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('parentId', {
            'name': 'parentId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('parent', {
            'name': 'parent',
            'is_list': False,
            'optional': True,
            'type': 'models.Category',
            'is_relational': True,
            'documentation': None,
        }),
        ('children', {
            'name': 'children',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Category\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('products', {
            'name': 'products',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ProductCategory\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('path', {
            'name': 'path',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)

_ProductCategory_relational_fields: Set[str] = {
        'product',
        'category',
    }
_ProductCategory_fields: Dict['types.ProductCategoryKeys', PartialModelField] = OrderedDict(
    [
        ('productId', {
            'name': 'productId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('categoryId', {
            'name': 'categoryId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('featured', {
            'name': 'featured',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('sortOrder', {
            'name': 'sortOrder',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('product', {
            'name': 'product',
            'is_list': False,
            'optional': True,
            'type': 'models.Product',
            'is_relational': True,
            'documentation': None,
        }),
        ('category', {
            'name': 'category',
            'is_list': False,
            'optional': True,
            'type': 'models.Category',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Attribute_relational_fields: Set[str] = {
        'values',
    }
_Attribute_fields: Dict['types.AttributeKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('slug', {
            'name': 'slug',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('type', {
            'name': 'type',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('values', {
            'name': 'values',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ProductAttribute\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_ProductAttribute_relational_fields: Set[str] = {
        'product',
        'attribute',
    }
_ProductAttribute_fields: Dict['types.ProductAttributeKeys', PartialModelField] = OrderedDict(
    [
        ('productId', {
            'name': 'productId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('attributeId', {
            'name': 'attributeId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('valueText', {
            'name': 'valueText',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('valueNumber', {
            'name': 'valueNumber',
            'is_list': False,
            'optional': True,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('valueBool', {
            'name': 'valueBool',
            'is_list': False,
            'optional': True,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('product', {
            'name': 'product',
            'is_list': False,
            'optional': True,
            'type': 'models.Product',
            'is_relational': True,
            'documentation': None,
        }),
        ('attribute', {
            'name': 'attribute',
            'is_list': False,
            'optional': True,
            'type': 'models.Attribute',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_StockMovement_relational_fields: Set[str] = {
        'variant',
    }
_StockMovement_fields: Dict['types.StockMovementKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('variantId', {
            'name': 'variantId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('delta', {
            'name': 'delta',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('reason', {
            'name': 'reason',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('referenceId', {
            'name': 'referenceId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('variant', {
            'name': 'variant',
            'is_list': False,
            'optional': True,
            'type': 'models.ProductVariant',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_InventoryReservation_relational_fields: Set[str] = {
        'variant',
        'order',
    }
_InventoryReservation_fields: Dict['types.InventoryReservationKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('variantId', {
            'name': 'variantId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('orderId', {
            'name': 'orderId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('quantity', {
            'name': 'quantity',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('expiresAt', {
            'name': 'expiresAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('variant', {
            'name': 'variant',
            'is_list': False,
            'optional': True,
            'type': 'models.ProductVariant',
            'is_relational': True,
            'documentation': None,
        }),
        ('order', {
            'name': 'order',
            'is_list': False,
            'optional': True,
            'type': 'models.Order',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_WishlistItem_relational_fields: Set[str] = {
        'user',
        'product',
    }
_WishlistItem_fields: Dict['types.WishlistItemKeys', PartialModelField] = OrderedDict(
    [
        ('userId', {
            'name': 'userId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('productId', {
            'name': 'productId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('user', {
            'name': 'user',
            'is_list': False,
            'optional': True,
            'type': 'models.User',
            'is_relational': True,
            'documentation': None,
        }),
        ('product', {
            'name': 'product',
            'is_list': False,
            'optional': True,
            'type': 'models.Product',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Order_relational_fields: Set[str] = {
        'shippingAddress',
        'billingAddress',
        'items',
        'payments',
        'shipments',
        'partner',
        'customer',
        'user',
        'InventoryReservation',
    }
_Order_fields: Dict['types.OrderKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('orderNumber', {
            'name': 'orderNumber',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('userId', {
            'name': 'userId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('customerId', {
            'name': 'customerId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('partnerId', {
            'name': 'partnerId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('currencyCode', {
            'name': 'currencyCode',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('subtotal', {
            'name': 'subtotal',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('discountTotal', {
            'name': 'discountTotal',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('shippingTotal', {
            'name': 'shippingTotal',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('taxTotal', {
            'name': 'taxTotal',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('total', {
            'name': 'total',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': 'enums.OrderStatus',
            'is_relational': False,
            'documentation': None,
        }),
        ('paymentStatus', {
            'name': 'paymentStatus',
            'is_list': False,
            'optional': False,
            'type': 'enums.PaymentStatus',
            'is_relational': False,
            'documentation': None,
        }),
        ('fulfillment', {
            'name': 'fulfillment',
            'is_list': False,
            'optional': False,
            'type': 'enums.FulfillmentStatus',
            'is_relational': False,
            'documentation': None,
        }),
        ('shippingAddressId', {
            'name': 'shippingAddressId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('billingAddressId', {
            'name': 'billingAddressId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('shippingAddress', {
            'name': 'shippingAddress',
            'is_list': False,
            'optional': True,
            'type': 'models.Address',
            'is_relational': True,
            'documentation': None,
        }),
        ('billingAddress', {
            'name': 'billingAddress',
            'is_list': False,
            'optional': True,
            'type': 'models.Address',
            'is_relational': True,
            'documentation': None,
        }),
        ('customerEmail', {
            'name': 'customerEmail',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('customerName', {
            'name': 'customerName',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('paymentMode', {
            'name': 'paymentMode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('sendingAgent', {
            'name': 'sendingAgent',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('city', {
            'name': 'city',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('location', {
            'name': 'location',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('items', {
            'name': 'items',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.OrderItem\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('payments', {
            'name': 'payments',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Payment\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('shipments', {
            'name': 'shipments',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Shipment\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('partner', {
            'name': 'partner',
            'is_list': False,
            'optional': True,
            'type': 'models.Partner',
            'is_relational': True,
            'documentation': None,
        }),
        ('customer', {
            'name': 'customer',
            'is_list': False,
            'optional': True,
            'type': 'models.Customer',
            'is_relational': True,
            'documentation': None,
        }),
        ('user', {
            'name': 'user',
            'is_list': False,
            'optional': True,
            'type': 'models.User',
            'is_relational': True,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('canceledAt', {
            'name': 'canceledAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('InventoryReservation', {
            'name': 'InventoryReservation',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.InventoryReservation\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_OrderItem_relational_fields: Set[str] = {
        'order',
        'product',
        'variant',
        'shipments',
    }
_OrderItem_fields: Dict['types.OrderItemKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('orderId', {
            'name': 'orderId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('productId', {
            'name': 'productId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('variantId', {
            'name': 'variantId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('sku', {
            'name': 'sku',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('unitPrice', {
            'name': 'unitPrice',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('quantity', {
            'name': 'quantity',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('taxRate', {
            'name': 'taxRate',
            'is_list': False,
            'optional': True,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('discount', {
            'name': 'discount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('total', {
            'name': 'total',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('order', {
            'name': 'order',
            'is_list': False,
            'optional': True,
            'type': 'models.Order',
            'is_relational': True,
            'documentation': None,
        }),
        ('product', {
            'name': 'product',
            'is_list': False,
            'optional': True,
            'type': 'models.Product',
            'is_relational': True,
            'documentation': None,
        }),
        ('variant', {
            'name': 'variant',
            'is_list': False,
            'optional': True,
            'type': 'models.ProductVariant',
            'is_relational': True,
            'documentation': None,
        }),
        ('shipments', {
            'name': 'shipments',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ShipmentItem\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Payment_relational_fields: Set[str] = {
        'order',
    }
_Payment_fields: Dict['types.PaymentKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('orderId', {
            'name': 'orderId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('provider', {
            'name': 'provider',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('amount', {
            'name': 'amount',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('currencyCode', {
            'name': 'currencyCode',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': False,
            'type': 'enums.PaymentStatus',
            'is_relational': False,
            'documentation': None,
        }),
        ('transactionId', {
            'name': 'transactionId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('rawData', {
            'name': 'rawData',
            'is_list': False,
            'optional': True,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('order', {
            'name': 'order',
            'is_list': False,
            'optional': True,
            'type': 'models.Order',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Shipment_relational_fields: Set[str] = {
        'items',
        'order',
    }
_Shipment_fields: Dict['types.ShipmentKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('orderId', {
            'name': 'orderId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('carrier', {
            'name': 'carrier',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('trackingCode', {
            'name': 'trackingCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('trackingUrl', {
            'name': 'trackingUrl',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('status', {
            'name': 'status',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('shippedAt', {
            'name': 'shippedAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('deliveredAt', {
            'name': 'deliveredAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('items', {
            'name': 'items',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ShipmentItem\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('order', {
            'name': 'order',
            'is_list': False,
            'optional': True,
            'type': 'models.Order',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_ShipmentItem_relational_fields: Set[str] = {
        'shipment',
        'orderItem',
    }
_ShipmentItem_fields: Dict['types.ShipmentItemKeys', PartialModelField] = OrderedDict(
    [
        ('shipmentId', {
            'name': 'shipmentId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('orderItemId', {
            'name': 'orderItemId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('quantity', {
            'name': 'quantity',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('shipment', {
            'name': 'shipment',
            'is_list': False,
            'optional': True,
            'type': 'models.Shipment',
            'is_relational': True,
            'documentation': None,
        }),
        ('orderItem', {
            'name': 'orderItem',
            'is_list': False,
            'optional': True,
            'type': 'models.OrderItem',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Tag_relational_fields: Set[str] = {
        'products',
    }
_Tag_fields: Dict['types.TagKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('slug', {
            'name': 'slug',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('products', {
            'name': 'products',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ProductTag\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_ProductTag_relational_fields: Set[str] = {
        'product',
        'tag',
    }
_ProductTag_fields: Dict['types.ProductTagKeys', PartialModelField] = OrderedDict(
    [
        ('productId', {
            'name': 'productId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('tagId', {
            'name': 'tagId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('product', {
            'name': 'product',
            'is_list': False,
            'optional': True,
            'type': 'models.Product',
            'is_relational': True,
            'documentation': None,
        }),
        ('tag', {
            'name': 'tag',
            'is_list': False,
            'optional': True,
            'type': 'models.Tag',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Coupon_relational_fields: Set[str] = set()  # pyright: ignore[reportUnusedVariable]
_Coupon_fields: Dict['types.CouponKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('code', {
            'name': 'code',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('type', {
            'name': 'type',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('value', {
            'name': 'value',
            'is_list': False,
            'optional': False,
            'type': 'decimal.Decimal',
            'is_relational': False,
            'documentation': None,
        }),
        ('currencyCode', {
            'name': 'currencyCode',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('startsAt', {
            'name': 'startsAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('endsAt', {
            'name': 'endsAt',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('maxRedemptions', {
            'name': 'maxRedemptions',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('timesRedeemed', {
            'name': 'timesRedeemed',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('isActive', {
            'name': 'isActive',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)

_Review_relational_fields: Set[str] = {
        'product',
        'user',
    }
_Review_fields: Dict['types.ReviewKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('productId', {
            'name': 'productId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('userId', {
            'name': 'userId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('rating', {
            'name': 'rating',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('title', {
            'name': 'title',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('body', {
            'name': 'body',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('isPublished', {
            'name': 'isPublished',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('product', {
            'name': 'product',
            'is_list': False,
            'optional': True,
            'type': 'models.Product',
            'is_relational': True,
            'documentation': None,
        }),
        ('user', {
            'name': 'user',
            'is_list': False,
            'optional': True,
            'type': 'models.User',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Customer_relational_fields: Set[str] = {
        'address',
        'orders',
        'user',
    }
_Customer_fields: Dict['types.CustomerKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('email', {
            'name': 'email',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('phone', {
            'name': 'phone',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('group', {
            'name': 'group',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('addressId', {
            'name': 'addressId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('address', {
            'name': 'address',
            'is_list': False,
            'optional': True,
            'type': 'models.Address',
            'is_relational': True,
            'documentation': None,
        }),
        ('city', {
            'name': 'city',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('country', {
            'name': 'country',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('orders', {
            'name': 'orders',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Order\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('userId', {
            'name': 'userId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('user', {
            'name': 'user',
            'is_list': False,
            'optional': True,
            'type': 'models.User',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Partner_relational_fields: Set[str] = {
        'address',
        'orders',
    }
_Partner_fields: Dict['types.PartnerKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('type', {
            'name': 'type',
            'is_list': False,
            'optional': False,
            'type': 'enums.PartnerType',
            'is_relational': False,
            'documentation': None,
        }),
        ('contactName', {
            'name': 'contactName',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('phone', {
            'name': 'phone',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('email', {
            'name': 'email',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('addressId', {
            'name': 'addressId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('address', {
            'name': 'address',
            'is_list': False,
            'optional': True,
            'type': 'models.Address',
            'is_relational': True,
            'documentation': None,
        }),
        ('orders', {
            'name': 'orders',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Order\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('headerTitle', {
            'name': 'headerTitle',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('footerNote', {
            'name': 'footerNote',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('bankDetails', {
            'name': 'bankDetails',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('contactInfo', {
            'name': 'contactInfo',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('logoUrl', {
            'name': 'logoUrl',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('paymentTerms', {
            'name': 'paymentTerms',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('returnPolicy', {
            'name': 'returnPolicy',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)



# we have to import ourselves as relation types are namespaced to models
# e.g. models.Post
from . import models, actions

# required to support relationships between models
model_rebuild(User)
model_rebuild(PasswordResetToken)
model_rebuild(Address)
model_rebuild(UserAddress)
model_rebuild(Product)
model_rebuild(ProductImage)
model_rebuild(ProductVariant)
model_rebuild(ProductOption)
model_rebuild(ProductOptionValue)
model_rebuild(ProductVariantOptionValue)
model_rebuild(Category)
model_rebuild(ProductCategory)
model_rebuild(Attribute)
model_rebuild(ProductAttribute)
model_rebuild(StockMovement)
model_rebuild(InventoryReservation)
model_rebuild(WishlistItem)
model_rebuild(Order)
model_rebuild(OrderItem)
model_rebuild(Payment)
model_rebuild(Shipment)
model_rebuild(ShipmentItem)
model_rebuild(Tag)
model_rebuild(ProductTag)
model_rebuild(Coupon)
model_rebuild(Review)
model_rebuild(Customer)
model_rebuild(Partner)
